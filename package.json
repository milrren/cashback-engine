{
  "name": "cashback-engine",
  "version": "1.0.0",
  "description": "High-performance Real-Time Cashback Engine using DDD and FP.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean",
    "build": "tsc",
    "start": "node dist/index.js",
    "infra:up": "docker compose up -d",
    "infra:down": "docker compose down",
    "infra:logs": "docker compose logs -f",
    "infra:setup-topics": "docker exec -it cashback_kafka /opt/kafka/bin/kafka-topics.sh --create --topic purchase.events.confirmed --bootstrap-server localhost:9092 --partitions 1 --replication-factor 1 && docker exec -it cashback_kafka /opt/kafka/bin/kafka-topics.sh --create --topic cashback.events.granted --bootstrap-server localhost:9092 --partitions 1 --replication-factor 1",
    "db:seed": "tsx scripts/seed-db.ts",
    "test:event": "tsx scripts/test-event.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "keywords": [],
  "author": "Milrren Mattar",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.0.9",
    "@types/uuid": "^10.0.0",
    "@vitest/coverage-v8": "^4.0.17",
    "ts-node": "^10.9.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.17"
  },
  "dependencies": {
    "avsc": "^5.7.9",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "kafkajs": "^2.2.4",
    "mongodb": "^7.0.0",
    "uuid": "^13.0.0"
  }
}
